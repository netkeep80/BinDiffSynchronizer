#pragma once
// pstring.h — Алиас для совместимости: делегирует в pstring_pmm.h (Задача 15.4).
//
// После консолидации (Фаза 15) каноническая реализация персистной строки
// находится в pstring_pmm.h (namespace pjson). Этот файл предоставляет имя
// pstring без суффикса _pmm для обратной совместимости.
//
// @see pstring_pmm.h — каноническая реализация
// @see plan.md Задача 15.4 — Консолидация pstring.h и pstring_pmm.h
//
// Все комментарии — на русском языке (Тр.6).

#include "pstring_pmm.h"
#include "fptr_pmm.h"

using namespace pjson;

// ═══════════════════════════════════════════════════════════════════════════
// Алиас типа (Задача 15.4)
// ═══════════════════════════════════════════════════════════════════════════

/// pstring — алиас для pjson::pstring_pmm (обратная совместимость).
using pstring = pjson::pstring_pmm;

// Проверки размера (обратная совместимость).
static_assert( sizeof( pstring ) == 2 * sizeof( void* ),
               "pstring должна занимать 2 * sizeof(void*) байт (Phase 3)" );
